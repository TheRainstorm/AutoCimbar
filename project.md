打算做一个类似 [libcimbar](https://github.com/sz3/libcimbar) 的项目，将文件自动转换成一系列二维码，自动播放，接收端自动扫描，并转换回文件。
和 libcimbar 使用手机摄像头扫描不同，本项目针对通过远程桌面截屏目标计算机的场景。由于不需要通过物理摄像头信道，因此单个二维码编码的信息可以远高于 libcimbar。

- 想要实现在合理时间传输 MB 级别的文件，速度需要 > 106 KB/s。
- 可以使用 go 或者 python 编程语言
 - 但是要支持 windows，方便分发（不想遇到 dll 不兼容的问题）
- encoder 和 decode 可以使用不同语言
- encoder 支持浏览器（可以最后再做）

技术原理核心

- 物理层：一张二维码图片支持一定大小的数据 B。不一定是标准二维码，可以是 libcimbar 实现的包含颜色、形状的高信息密度二维码
  - 还可以牺牲一定原始数据，采用 ECC 纠错，在图片识别错误一小部分时，不丢弃整张图片，从而提升传输速率
- encoder 输入一个文件，大小为 N，使用喷泉码（wirehair）产生不同的二维码，每个二维码包含一个片段，大小 b。由于喷泉码的特性，接收到大概 N/b + 1|2 张二维码后就可以恢复源数据。
- 整个过程因为是一个单向信道，因此二维码中只放真正的数据。像 ecc 参数、二维码参数（大小、颜色通道、形状通道）、喷泉码参数（比如最大大小）这些参数只要两边统一提前设置好即可，不占用带宽去每次发送。
- 喷泉码通常对输入数据大小有限制，将这个大小变为一个可以配置的参数，最好是运行时可以配置的
- 编码器使用多进程甚至 gpu 加速，尽可能提升输出速度
- 解码器使用高性能的截屏让数据输入不是瓶颈，并且实现高性能的解码器
 - 采用多进程
 - 甚至 gpu 加速
 - 输入和解码相互异步，延迟隐藏
- encoder 会在屏幕特定位置不断更新二维码，需要可以指定大小和位置。
- decoder 也是可以指定在屏幕特定位置截图，进行解码（指定大小而不是全屏截图可以增加截图速度）
- Q 表示类似于 QR 码那样的版本，越大的 Q 像素越多，数据量越大。
- B 表示用多少像素代表原来一个像素，表示整体缩放
- R 指定发送和接收位置，decode 第一个数字表示屏幕编号。-0:-0 表示从右下角作为原点，偏移 0 和 0，也就是二维码贴近右下角。
```
python encoder.py -i in.txt -Q 14 -B 2 -R -0:-0
python decoder.py -Q 14 -B 2 -R 2:-0:-0
```

- libcimbar 中颜色通道是 2-3 bit，形状通道是 4bit，每个 cell 大小是 4x4 像素。我们这里的像素还需要通过 B 来缩放控制实际对应屏幕像素是多少。
- 二维码中的定位标记可以丢掉。我们的场景encode 和 decode 区域是手动指定的不需要定位标记。